# MerchNet

Modern Retail Billing Software.

## Quick Start

1. Install dependencies: `npm install`
2. Set up your `.env.local` using `.env.example` as a template
3. Start the development server: `npm run dev`
